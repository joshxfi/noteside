---
title: Configuration
description: Theme, typography, and the ~/.notesiderc config file.
---

Everything is configurable from a live **`~/.notesiderc`** buffer (vimrc-flavored, in
your home directory) or the in-app **Settings** panel — they're two editors over the
same config. Open the file with `:config`, edit any line, and `:w` to apply.

## Example

```vim
" ~/.notesiderc — Noteside configuration

" appearance
set theme        = light
set accent       = plum

" typography
set editor-font  = Newsreader
set ui-font      = IBM Plex Mono
set font-size    = 19
set line-height  = 1.75

" cursor
set cursor       = block
set cursor-blink = on

" editor
set live-preview = on

" keys
set vim          = on
imap jj <Esc>
bind Ctrl-j find
```

## Settings

| Setting        | Values                                                           |
| -------------- | ---------------------------------------------------------------- |
| `theme`        | `light` · `dark`                                                 |
| `accent`       | `terracotta` · `ochre` · `sage` · `dusk` · `plum` · `monochrome` |
| `editor-font`  | `Newsreader` · `Spectral` · `IBM Plex Mono` · `JetBrains Mono`   |
| `ui-font`      | `IBM Plex Mono` · `JetBrains Mono` · `Space Mono`                |
| `font-size`    | `14`–`28`                                                        |
| `line-height`  | `1.4`–`2.1`                                                      |
| `cursor`       | `block` · `bar` · `underline`                                    |
| `cursor-blink` | `on` · `off`                                                     |
| `live-preview` | `on` · `off`                                                     |
| `vim`          | `on` · `off`                                                     |

## Appearance

- **Theme** — `light` or `dark`; a warm paper reading surface either way.
- **Accent** — one quiet color, used for the cursor, headings, links, and selection:
  `terracotta`, `ochre`, `sage`, `dusk`, `plum` (the default), or `monochrome`.
- **Typography** — `editor-font` sets the writing surface (serif `Newsreader` or
  `Spectral`, or mono `IBM Plex Mono` / `JetBrains Mono`); `ui-font` sets the surrounding UI.
  `font-size` (14–28) and `line-height` (1.4–2.1) tune the reading rhythm.

## Key mappings

- **Vim mappings** — `nmap` / `imap` / `vmap` lines apply on `:w` (e.g. `imap jj <Esc>`).
- **Chord rebinds** — `bind <chord> <command-id>` remaps a conventional chord;
  `bind none <command-id>` unbinds it.

The command ids are: `find`, `grep`, `search`, `commands`, `backlinks`, `new`,
`save`, `saveQuit`, `quit`, `delete`, `follow`, `nav`, `togglePreview`, `settings`,
`config`, `cheatsheet`. For example, `bind Ctrl-j find` puts the finder on `Ctrl-J`.
